{
  "name": "Editor",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build-linux": "electron-packager . --overwrite --platform=linux --arch=x64 --icon=icon.icns --prune=true --out=release-builds",
    "build-osx": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=icon.icns --prune=true --out=release-builds",
    "build-windows": "electron-packager . --overwrite --platform=win32 --arch=x64 --icon=icon.icns --prune=true --out=release-builds",
    "start": "electron ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.2.9",
    "electron-packager": "^12.2.0"
  },
  "dependencies": {
  }
}
